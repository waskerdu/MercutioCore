#pragma once
#include <string>
#include <vector>

int FirstCharIndex(std::string str, int startIndex = 0);
int GetWordLeng(std::string str, int startIndex = 0);
void Parser(std::string str, std::vector<std::string>& strings);
